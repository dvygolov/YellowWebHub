{
  "name": "yellowweb-hub",
  "version": "200526b1",
  "private": true,
  "description": "Yellow Web bookmarklet tools hub for Cloudflare Pages.",
  "scripts": {
    "check": "node --check noop-check.js",
    "build": "node noop-check.js",
    "deploy": "npx wrangler pages deploy ./dist --project-name=yellowweb --branch=main --commit-dirty=true"
  },
  "author": "Yellow Web",
  "license": "UNLICENSED",
  "devDependencies": {}
}
